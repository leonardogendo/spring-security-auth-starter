spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/auth_starter}
    username: ${DB_USERNAME:demo_user}
    password: ${DB_PASSWORD:demouser1165}
  
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG